name: Sync Backend to Hugging Face Space

on:
  push:
    branches: [main]
<<<<<<< HEAD
=======
    paths:
      - '/**'
>>>>>>> 4c03053d63b2e616165c70662cb606321dd68725
  workflow_dispatch:

jobs:
  sync-to-hub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          lfs: true

      - name: Push to Hugging Face Space
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_USERNAME: ${{ secrets.HF_USERNAME }}
          # Assumes Space name is 'templeapp-backend' as per setup guide
          SPACE_NAME: templeapp-backend 
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions"
          
          # Add Hugging Face Remote
          # We use the token for authentication
          git remote add space https://$HF_USERNAME:$HF_TOKEN@huggingface.co/spaces/$HF_USERNAME/$SPACE_NAME
          
          # Push the current branch (main) to the Space
          git push -f space main
